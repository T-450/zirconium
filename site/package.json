{
  "name": "zirconium-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "node ../scripts/generate.mjs --check && vite build",
    "test:prefix": "node tests/prefix-smoke.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.0",
    "svelte": "^5.30.0",
    "vite": "^6.3.5"
  }
}
