use flake

# Point Neovim at this repository's development init.lua without touching
# the user's regular editor configuration or global Git configuration.
export XDG_CONFIG_HOME="$(dirname "$PWD")"
export NVIM_APPNAME=zirconium
